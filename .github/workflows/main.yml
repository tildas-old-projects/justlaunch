name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - if: "!contains(github.event.head_commit.message, '[skip]')"
    - uses: actions/checkout@v1
    - name: Build
      run: |
        cargo build --verbose
       
